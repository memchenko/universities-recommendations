import { Injectable } from '@nestjs/common';
{{#if isCrud}}
import { TypeOrmCrudService } from '@nestjsx/crud-typeorm';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';

import {{moduleName.kebabCased}}Entity from './{{moduleName.dashed}}.entity';
{{/if}}

@Injectable()
export default class {{moduleName.kebabCased}}Service {{#if isCrud}}extends TypeOrmCrudService<{{moduleName.kebabCased}}Entity>{{/if}}{
    {{#if isCrud}}
    constructor(
        @InjectRepository({{moduleName.kebabCased}}Entity)
        readonly repository: {{moduleName.kebabCased}}Entity,
    ) {
        super(repository);
    }
    {{/if}}
}
